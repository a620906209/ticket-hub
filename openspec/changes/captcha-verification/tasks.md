## 1. Application 層基礎建設

- [ ] 1.1 `ProjectC.Application/Common/Interfaces/ICaptchaService.cs`：定義 `GenerateAsync(CancellationToken)` 回傳 `CaptchaChallenge`（token、PNG bytes）、`VerifyAsync(string token, string answer, CancellationToken)` 回傳 `bool`，兩者皆接受並向下傳遞 `CancellationToken`（比照 `IQueryCache` 慣例；**這是外部可觀察契約，不是裝飾性簽章，對應 CAPTCHA-CANCEL-001／002／003／004，見 design.md 決策 10、2.3、5.1、5.2、5.2a、5.5、5.6 的對應測試要求**）。**介面放置於 `Application/Common/Interfaces`（非 `Domain`）是刻意決策，不是疏漏**：比照 `IDistributedLock`／`IDateTimeProvider`／`IQueryCache` 這三個「純技術性、不承載業務語意」外部服務介面的既有放置慣例，見 design.md 決策 12 的完整判準與替代方案分析。`CaptchaChallenge` 同理放同目錄（或其下 `Captcha` 子資料夾），不放 `Domain`
- [ ] 1.2 `ProjectC.Application/Common/CaptchaOptions.cs`：僅 `TtlSeconds`（`public int TtlSeconds { get; set; } = 120;`，MUST 加 `[Range(1, int.MaxValue)]`）——**MUST 比照 `CaptchaRateLimitingOptions`／`LoginRateLimitingOptions` 的驗證模式（有預設值＋擋無效值），不是 `QueryCacheOptions`（無預設值，缺漏即 fail-fast）**，兩者互斥、不得混用，見 design.md 決策 14 的完整比對與理由；對應 spec 新增的 CAPTCHA-STORE-003／004。**驗證碼固定為 4 碼英數字（design.md 決策 2、spec 已明確寫死 4 碼），MUST NOT 提供可設定的 `Length`**：若同時存在「spec 寫死 4 碼」與「設定檔可調整長度」兩套契約，設定 `Length=6` 時啟動驗證會通過但系統行為違反 spec；之後若真的需要可設定長度，須同步改 spec／design／所有寫死 4 碼的測試斷言，屬於另一個 change 的範疇
- [ ] 1.3 `LoginRequest`（`ProjectC.Application/Authentication/Login/LoginRequest.cs`）與 `RegisterMemberRequest`（`ProjectC.Application/Members/Register/RegisterMemberRequest.cs`，**注意實際類別名稱是 `RegisterMemberRequest`，不是 `RegisterRequest`**）新增 `CaptchaToken`、`CaptchaAnswer` 兩個必填欄位，對應 FluentValidation Validator 新增規則：**MUST 用 `RuleFor(x => x.CaptchaAnswer == null ? null : x.CaptchaAnswer.Trim())` 這種在 `RuleFor` lambda 內直接轉換值的寫法建立規則鏈（並用 `.OverridePropertyName(nameof(...))` 維持錯誤訊息對應正確欄位名稱），再串接 `NotEmpty()` 與 `MaximumLength()`，不得對未 trim 的原始字串做長度檢查，也 MUST NOT 使用 `.Transform()`（`FluentValidation 12.1.1` 已移除此 API，見 design.md 決策 8 版本核對）**（`CaptchaToken` 加 `MaximumLength(64)`、`CaptchaAnswer` 加 `MaximumLength(16)`）——先驗證非空白（缺漏或 trim 後僅空白回 `Validation` 錯誤，對應 CAPTCHA-AUTH-002／CAPTCHA-REG-002），超過 trim 後長度上限同樣視為 `Validation` 錯誤（CAPTCHA-INPUT-001／002／004；design.md 決策 8）。**注意此寫法只影響驗證鏈內部判斷用的值，不會改寫 `CaptchaToken`／`CaptchaAnswer` 屬性本身**，`RedisCaptchaService.VerifyAsync`（2.3）仍必須自己對傳入的 `answer` 呼叫 `Trim()`。**`POST /api/events/{id}/queue/entries` 目前完全沒有 request body**（`EventQueueController.JoinQueue(Guid id, CancellationToken)`、`JoinPurchaseQueueHandler.HandleAsync(Guid eventId, Guid memberId, CancellationToken)` 皆不接受 DTO）：需新建 `JoinPurchaseQueueRequest(string CaptchaToken, string CaptchaAnswer)` record（`ProjectC.Application/PurchaseQueue/JoinPurchaseQueue/`），`EventQueueController.JoinQueue` 新增 `[FromBody] JoinPurchaseQueueRequest request` 參數並傳給 Handler，`JoinPurchaseQueueHandler.HandleAsync` 簽章改為 `(Guid eventId, Guid memberId, JoinPurchaseQueueRequest request, CancellationToken cancellationToken)`（新增 FluentValidation Validator，同樣規則：`RuleFor` lambda 內直接 trim 再 `NotEmpty()`＋長度上限，處理缺漏欄位對應 CAPTCHA-QUEUE-002、超長對應 CAPTCHA-INPUT-001／002／004）
- [ ] 1.4 `LoginHandler`／`RegisterMemberHandler`／`JoinPurchaseQueueHandler`：在既有帳密／唯一性／排隊資格檢查**之前**，先呼叫 `ICaptchaService.VerifyAsync`；驗證失敗回傳 `Result.Failure(Error.Validation(...))`，不繼續執行後續查詢或交易（對應 CAPTCHA-AUTH-001、CAPTCHA-REG-001、CAPTCHA-QUEUE-001）
- [ ] 1.5 `GetCaptchaHandler`：呼叫 `ICaptchaService.GenerateAsync`，回傳 token 與 base64 PNG（對應 CAPTCHA-GEN-001／002）

## 2. Infrastructure 實作

- [ ] 2.1 `Directory.Packages.props` 第一個（正式）`ItemGroup` 新增套件參考並鎖定版本：`SixLabors.ImageSharp.Drawing` `1.0.0`、`SixLabors.Fonts` `1.0.0`（皆為 Apache 2.0，與既有 `SixLabors.ImageSharp 2.1.11` 相容，MUST NOT 使用 3.x 系列，見 design.md 決策 1 的授權鎖定說明），`ProjectC.Infrastructure.csproj` 新增對應 `<PackageReference>`（純受管理程式碼，比照 `TicketQrCodeGenerator` 不依賴 `System.Drawing` 的既有慣例）
- [ ] 2.1a 內嵌驗證碼繪製用字型：將 `DejaVu Sans`（`DejaVu Fonts License`，見 design.md 決策 1「選用字型與授權條款」）字型檔以 embedded resource 加入 `ProjectC.Infrastructure`（`.csproj` 設定 `<EmbeddedResource Include="...">`，字型檔與其官方授權條款文字檔一併放入同一目錄，例如 `ProjectC.Infrastructure/Captcha/Fonts/DejaVuSans.ttf` 與 `LICENSE`），啟動或首次使用時透過 `FontCollection.Add(stream)` 載入取得 `FontFamily`，MUST NOT 使用 `SystemFonts`（design.md 決策 1：容器 base image 不內建系統字型，`SystemFonts` 在容器內會拋出 `FontException`）；MUST NOT 修改字型檔本身（design.md 決策 1 授權限制：修改後重新散布不得沿用 `DejaVu`／`Bitstream`／`Vera` 檔名）
- [ ] 2.2 `ProjectC.Infrastructure/Captcha/ICaptchaImageGenerator.cs`（介面）與 `CaptchaImageGenerator.cs`（實作）：產生 4 碼英數字內容（字元集排除 `0`/`O`、`1`/`I`/`l`），繪製為 PNG bytes（含基本雜訊線干擾），文字繪製使用 2.1a 內嵌的 `FontFamily`。**MUST 是介面而非直接由 `RedisCaptchaService` 建構具體類別**（design.md 決策 10「可測試性」段落）：這是 Infrastructure 內部的技術性測試替身邊界，讓測試能注入假的圖片產生器、控制「圖片產生完成的那一刻讓 token 被取消」這個時間點（見 5.2a），與決策 12 的跨層介面放置規則無關
- [ ] 2.3 `ProjectC.Infrastructure/Captcha/RedisCaptchaService.cs`：實作 `ICaptchaService`（建構子注入 2.2 的 `ICaptchaImageGenerator`）——`GenerateAsync`／`VerifyAsync` **開頭 MUST 各自呼叫一次 `cancellationToken.ThrowIfCancellationRequested()`**（比照 `RedisQueryCache.cs` 既有慣例：`StackExchange.Redis` 的 `IDatabaseAsync` 非同步方法本身不接受 `CancellationToken`，MUST 靠這個手動呼叫讓已取消的呼叫在送出 Redis 命令前就拋出，不得省略；design.md 決策 10、CAPTCHA-CANCEL-001／002）。**`GenerateAsync` MUST 額外在呼叫 `StringSetAsync` 之前再檢查一次 `cancellationToken.ThrowIfCancellationRequested()`**（開頭檢查與 `StringSetAsync` 之間還有 `ICaptchaImageGenerator` 產生 PNG 這段 CPU-bound 耗時工作，取消可能發生在這段期間，開頭檢查一次不足以攔截，design.md 決策 10、CAPTCHA-CANCEL-004）；同理 **`VerifyAsync` MUST 在呼叫 `StringGetDeleteAsync` 之前再檢查一次**，即使中間只有輕量的 trim／雜湊運算，仍比照相同模式避免遺漏（design.md 決策 10）。`GenerateAsync` 呼叫 `ICaptchaImageGenerator` 產生內容與圖片，答案正規化大寫後 SHA-256 雜湊寫入 Redis（`captcha:{token}`，TTL 依 `CaptchaOptions.TtlSeconds`）；`VerifyAsync` 對傳入的 `answer` 先 `Trim()` 再正規化大寫、計算雜湊（design.md 決策 2／CAPTCHA-INPUT-003），**MUST 使用單一 `StringGetDeleteAsync`（`GETDEL`）呼叫**同時讀取並刪除該 key，MUST NOT 拆成 `StringGetAsync` 後再 `KeyDeleteAsync` 兩步（design.md 決策 4：兩步寫法在並發驗證下會產生競態，讓同一 token 被兩個請求都驗證成功）（對應 CAPTCHA-STORE-001／002、CAPTCHA-VERIFY-001~006）
- [ ] 2.4 `RedisCaptchaService` MUST NOT 捕捉 Redis 連線／逾時例外——**與 `RedisQueryCache`／`RedisDistributedLock` 的 fail-open 慣例相反**，讓例外原樣往外拋，交由既有全域 `IExceptionHandler` 轉換為技術性錯誤回應（對應 CAPTCHA-FAIL-001／002；design.md 決策 5）
- [ ] 2.5 `CaptchaOptions` 啟動時 fail-fast 驗證：`TtlSeconds` 須為正整數，**MUST 比照 `CaptchaRateLimitingOptions`／`LoginRateLimitingOptions` 的模式（`[Range(1, int.MaxValue)]` ＋ 有預設值 ＋ MUST NOT 加 `.ValidateOnStart()`，改用 `app.Build()` 後強制解析觸發 fail-fast），不是 `QueryCacheOptions` 的模式（見 design.md 決策 14）**
- [ ] 2.6 `Program.cs`：註冊 `ICaptchaService` → `RedisCaptchaService`（**Scoped**）、`ICaptchaImageGenerator` → `CaptchaImageGenerator`（**Scoped**，MUST NOT 是 Singleton——`SixLabors.Fonts` 的 `FontFamily`／`FontCollection` 未經查證取得多執行緒並行讀取的官方 thread-safety 保證，Scoped 讓每個 request 各自持有獨立執行個體，從根本排除疑慮，不依賴 `IConnectionMultiplexer` 剛好是 Singleton 這個無關的理由，見 design.md 決策 13）；`AddOptions<CaptchaOptions>().Bind(...).ValidateDataAnnotations()`（**MUST NOT** 加 `.ValidateOnStart()`）並解包成 Singleton，`app.Build()` 之後 MUST 新增 `app.Services.GetRequiredService<CaptchaOptions>();` 強制解析一次觸發 fail-fast（比照 `CaptchaRateLimitingOptions`／`LoginRateLimitingOptions` 既定寫法，design.md 決策 14）
- [ ] 2.6a **`Program.cs`（既有共用 `IConnectionMultiplexer` 註冊，`AbortOnConnectFail = false` 那幾行）MUST 新增明確逾時設定**：`redisConfigurationOptions.ConnectTimeout = 2000`、`SyncTimeout = 2000`、`AsyncTimeout = 2000`（三者皆毫秒）、`ConnectRetry = 1`（重試次數，非時間單位；design.md 決策 11）——這是**修改既有共用註冊**，不是新增 CAPTCHA 專屬的第二個 `IConnectionMultiplexer`，同時影響 `query-caching`／`purchase-queue-leader-election`／CAPTCHA 三種用途。修改後 MUST 實際執行 `query-caching`／`purchase-queue-leader-election` 既有測試套件（`docker compose exec api dotnet test`）確認全數仍通過，不能只憑「fail-open 路徑不在乎逾時長短」的理論推斷就視為不受影響
- [ ] 2.6b **`CaptchaOptionsTests`（`tests/ProjectC.Application.Tests/Common/`，逐行比照既有 `LoginRateLimitingOptionsTests.cs`／5.4a 的 `CaptchaRateLimitingOptionsTests`）**：驗證 `new CaptchaOptions()` 的預設值為 `TtlSeconds = 120`（CAPTCHA-STORE-003）
- [ ] 2.6c **`RateLimitingOptionsFailFastTests.cs`（`tests/ProjectC.WebApi.Tests/Startup/`）新增 `Captcha` 的理論測試方法，逐行比照既有 `CreatingHost_WithNonPositiveLoginRateLimitingValues_ThrowsOptionsValidationException`／6.2b**：`Captcha:TtlSeconds` 設為 0 或負數時，建立 Host 拋出 `OptionsValidationException`（CAPTCHA-STORE-004）

## 3. WebApi 層

- [ ] 3.1 新增 `CaptchaController`：`GET /api/captcha`（匿名可存取），呼叫 `GetCaptchaHandler`，回傳 `{ token, imageBase64 }`
- [ ] 3.1a `ProjectC.Application/Common/CaptchaRateLimitingOptions.cs`：新增獨立設定類別，比照 `LoginRateLimitingOptions.cs` 的既定寫法逐行對照——`SectionName = "CaptchaRateLimiting"`、`[Range(1, int.MaxValue)] PermitLimit` 預設 `10`、`[Range(1, int.MaxValue)] WindowSeconds` 預設 `60`（design.md 決策 6：**MUST 是獨立類別，不得與 `RateLimitingOptions`／`LoginRateLimitingOptions` 共用**，理由見該決策）
- [ ] 3.2 `Program.cs`：
  - 註冊 `CaptchaRateLimitingOptions`，比照既有 `RateLimitingOptions`／`LoginRateLimitingOptions` 寫法：`AddOptions<CaptchaRateLimitingOptions>().Bind(builder.Configuration.GetSection(CaptchaRateLimitingOptions.SectionName)).ValidateDataAnnotations()`（**MUST NOT** 加 `.ValidateOnStart()`，比照兩者皆有安全預設值、不需要 `ValidateOnStart` 的既有模式），並解包成 Singleton；`app.Build()` 之後 MUST 新增 `app.Services.GetRequiredService<CaptchaRateLimitingOptions>();` 強制解析一次以觸發 fail-fast（比照既有 `RateLimitingOptions`／`LoginRateLimitingOptions` 兩行既定寫法，缺這一步 DataAnnotations 驗證不會在啟動階段執行）
  - `rateLimiterOptions.AddPolicy("captcha", httpContext => CreateCaptchaPartition(httpContext))`：**MUST 是獨立的具名 policy，MUST NOT 傳入 `"login"` 這個既有 policy 名稱**（design.md 決策 6：共用 policy 名稱會讓兩個端點共用同一個 `PartitionedRateLimiter` 計數器）；`CreateCaptchaPartition` 這個 local function 重用既有 `LoginRateLimiterPartitioning.GetPartitionKey(httpContext)` 取得分區鍵字串，但改讀取 `CaptchaRateLimitingOptions`（不是 `LoginRateLimitingOptions`）算出 `FixedWindowRateLimiterOptions`，寫法比照既有 `CreateIpPartition` local function
  - `CaptchaController.Get` 套用 `[EnableRateLimiting("captcha")]`（對應 CAPTCHA-RATE-001／002／003／004）
- [ ] 3.3 `appsettings.json`／`appsettings.Development.json` 新增 `Captcha` 設定區塊（僅 `TtlSeconds`；驗證碼固定 4 碼，不提供可設定的 `Length`，見 1.2）與獨立的 `CaptchaRateLimiting` 設定區塊（`PermitLimit`、`WindowSeconds`，比照既有 `LoginRateLimiting` 區塊的既定寫法，兩個區塊不得合併或共用鍵名）

## 4. 前端（buyer-web-ui）

- [ ] 4.1 新增 `useCaptcha` composable：封裝呼叫 `GET /api/captcha`、暴露 `token`、`imageBase64`、`refresh()`；三處呼叫端共用（design.md 決策 7）。**新增 `loadError` 狀態**：`refresh()`（含初次載入時的呼叫）失敗時設定該狀態為固定提示文字「系統暫時無法取得驗證碼，請稍後再試」、不拋出未捕捉例外，成功時清空該狀態（design.md 決策 7，對應 CAPTCHA-BW-005／006）
- [ ] 4.2 `web/src/api/auth.ts` 的 `register(email, password, displayName)`／`login(email, password)` 簽章各自新增 `captchaToken`／`captchaAnswer` 兩個參數；`web/src/stores/auth.ts` 的 `login()`（Pinia store action，`LoginPage.test.ts` 目前 mock 的就是這個函式）與對應的 `register()` store action 同步新增這兩個參數並轉呼叫 `authApi`；`auth.test.ts` 既有呼叫 `login(email, password)` 的案例（「登入成功寫入 accessToken 與 member」等）MUST 同步補上驗證碼參數，否則會編譯失敗。註冊頁：整合驗證碼圖片、輸入欄位，送出時附帶 `token`／使用者輸入；驗證碼錯誤時顯示提示並自動 `refresh()`（對應 CAPTCHA-BW-001／002／003）；`useCaptcha` 的 `loadError` 有值時顯示該提示、停用送出按鈕，並提供重試操作重新呼叫 `refresh()`（對應 CAPTCHA-BW-005／006）
- [ ] 4.3 登入頁：整合驗證碼元件，行為同註冊頁，載入時顯示驗證碼圖片與輸入欄位（對應 CAPTCHA-BW-004，已於 buyer-web-ui spec 補上此 Scenario）；`loadError` 處理同註冊頁（CAPTCHA-BW-005／006）；與既有 429 友善提示（`LRL-009`）互不干擾，各自獨立判斷
- [ ] 4.4 `web/src/api/queue.ts` 的 `joinQueue(eventId: string)` 簽章改為 `joinQueue(eventId: string, captchaToken: string, captchaAnswer: string)`，並更新 request body。活動詳情頁「加入排隊」操作：尚未加入排隊的畫面新增驗證碼元件，加入排隊請求附帶 token／輸入；驗證碼錯誤時顯示提示、自動換發，停留在原畫面，不觸發座位/票種資料的清空或重新整理（對應 CAPTCHA-BW-QUEUE-001／002）；`loadError` 處理沿用同一套共用行為（顯示提示、停用加入排隊按鈕、提供重試），不重複實作

## 5. 單元測試

- [ ] 5.1 `FakeCaptchaService`（比照既有 `FakeQueryCache`／`FakeDateTimeProvider` 手法）：實作 `ICaptchaService`，`GenerateAsync` 回傳固定 token（例如 `"11111111-1111-1111-1111-111111111111"`）與固定明碼答案（例如 `"TEST"`，測試專用常數對外公開），`VerifyAsync` 對該固定 token／答案回傳 `true`、其餘一律回傳 `false`；**兩個方法開頭都 MUST 呼叫 `cancellationToken.ThrowIfCancellationRequested()`**（design.md 決策 10、CAPTCHA-CANCEL-003）——這不是模擬真實 Redis 行為，而是讓 5.5 能用「傳入已取消的 token」這個既有測試手法驗證 Handler 是否真的把自己收到的 `cancellationToken` 轉傳給 `ICaptchaService`，而不是誤用 `CancellationToken.None`。**MUST 分別在三個測試專案各自建立一份，不得讓某個測試專案跨專案參照另一個測試專案的型別**（`ProjectC.Infrastructure.Tests.csproj`／`ProjectC.WebApi.Tests.csproj` 皆只 `ProjectReference` `src/` 底下的正式專案，彼此之間、與 `ProjectC.Application.Tests.csproj` 之間都沒有 `ProjectReference`，核對過現有 `FakeDateTimeProvider.cs`／`FakeQueryCache.cs` 確實是分別存在於 `tests/ProjectC.Application.Tests/TestSupport/` 與 `tests/ProjectC.Infrastructure.Tests/TestSupport/` 兩份各自獨立的檔案，這是本專案既有、經過確認的既定慣例，不是本次改動要新引入的例外）：
  - `tests/ProjectC.Application.Tests/TestSupport/FakeCaptchaService.cs`：供 5.5（`LoginHandlerTests`／`RegisterMemberHandlerTests`／`JoinPurchaseQueueHandlerTests`）、5.6（`GetCaptchaHandlerTests`）使用
  - `tests/ProjectC.Infrastructure.Tests/TestSupport/FakeCaptchaService.cs`：供 `JoinPurchaseQueueHandlerIntegrationTests.cs`（5.5a）使用
  - `tests/ProjectC.WebApi.Tests/TestSupport/FakeCaptchaService.cs`：供 6.2a（`CustomWebApplicationFactory`）、6.3、6.3a 使用
  - **三份檔案的固定 token／答案常數值 MUST 完全相同**，否則 6.2a 讓 `CustomWebApplicationFactory` 替換成 WebApi.Tests 專案自己的 Fake 之後，`AuthTestHelper` 組出的請求若誤用了 Application.Tests 那份 Fake 的常數值，會對不上實際生效的 Fake，驗證碼檢查會被判定失敗——**這是本次改動唯一需要「知道明碼答案」的測試替身，真正的 `RedisCaptchaService`（雜湊、不存明碼）行為只在 5.2／5.3 用真實 Redis 驗證，兩者不混用**
- [ ] 5.2 `RedisCaptchaServiceTests`（真實 Redis，`RedisFixture`，比照 `RedisQueryCacheTests` 手法）：GenerateAsync 寫入帶 TTL 的雜湊（CAPTCHA-STORE-001）；TTL 到期後驗證失敗（CAPTCHA-STORE-002，可用短 TTL + `Task.Delay` 驗證，或直接操作 Redis TTL 觀察）；答案正確驗證成功且 key 已被刪除（CAPTCHA-VERIFY-001）；答案錯誤驗證失敗且 key 已被刪除（CAPTCHA-VERIFY-002）；不存在的 token 驗證失敗（CAPTCHA-VERIFY-003）；同一 token 驗證兩次，第二次失敗（CAPTCHA-VERIFY-004）；大小寫不敏感比對成功（CAPTCHA-VERIFY-005）；輸入前後夾帶空白仍比對成功（CAPTCHA-INPUT-003，`VerifyAsync` 內部 `Trim()`）；**兩個並發呼叫（`Task.WhenAll`）以同一 token、同一正確答案同時呼叫 `VerifyAsync`，斷言恰好一次回傳 `true`、另一次回傳 `false`，不得兩者皆為 `true`（CAPTCHA-VERIFY-006，驗證 `GETDEL` 的原子性）**；**`GenerateAsync`／`VerifyAsync` 各自傳入已取消的 `CancellationTokenSource.Token`，斷言 `OperationCanceledException` 往外拋（比照既有 `RedisQueryCacheTests.GetAsync_WhenCancellationAlreadyRequested_ThrowsWithoutFailingOpen` 等三個既定案例的手法，design.md 決策 10，對應 CAPTCHA-CANCEL-001／002）**
- [ ] 5.2a **`RedisCaptchaServiceTests` 新增案例：`GenerateAsync` 在圖片產生完成後、Redis 寫入前才被取消（CAPTCHA-CANCEL-004，design.md 決策 10「可測試性」段落）**：`RedisCaptchaService` 建構子注入一個測試專用的假 `ICaptchaImageGenerator`（2.2 已改為介面），其產生圖片的方法內部呼叫測試自己持有的 `CancellationTokenSource.Cancel()` 後才回傳圖片位元組——藉此讓 `GenerateAsync` 呼叫時傳入的 token 在「方法開頭檢查已通過、圖片已產生完畢」的時間點才轉為已取消。斷言：呼叫拋出 `OperationCanceledException`；且真實 Redis 中該 token 對應的 key **不存在**（證明 `StringSetAsync` 從未被呼叫，取消確實發生在 Redis 寫入之前）——這是與 CAPTCHA-CANCEL-001（呼叫當下即已取消）不同的獨立情境，MUST 個別驗證，不能以 CAPTCHA-CANCEL-001 通過就視為涵蓋
- [ ] 5.3 `RedisCaptchaServiceFailClosedTests`（比照 `RedisQueryCacheTests` 的 `CreateThrowingConnection` 手法）：`GenerateAsync`／`VerifyAsync` 在 Redis 拋出 `RedisConnectionException`／`RedisTimeoutException` 時，例外原樣往外拋，不被吞掉、不回傳驗證通過（CAPTCHA-FAIL-001／002 的 Infrastructure 層級部分，`RedisTimeoutException` 案例對應 CAPTCHA-FAIL-003——比照既有 `RedisQueryCacheTests` 用 mock 拋出例外驗證行為、不用真實網路逾時這種難以決定性重現的時序的既定慣例；端到端層級的可觀察行為驗證見 6.4）
- [ ] 5.3a **`RedisConnectionConfigurationTests`（`tests/ProjectC.WebApi.Tests/Startup/`，比照既有 `RateLimitingOptionsFailFastTests` 手法）**：建立 Host 後取得已註冊的 `IConnectionMultiplexer`，以 `ConfigurationOptions.Parse(multiplexer.Configuration)` 解析回 `ConfigurationOptions`，斷言 `ConnectTimeout`／`SyncTimeout`／`AsyncTimeout` 皆為 `2000`、`ConnectRetry` 為 `1`（CAPTCHA-FAIL-003 的組態層級驗證：確認 2.6a 的顯式逾時設定確實生效，不依賴函式庫預設值；比照既有慣例用可決定性重現的方式驗證，不用真實逾時計時）
- [ ] 5.4 `CaptchaImageGeneratorTests`：產生內容為 4 碼、不含混淆字元集合、回傳非空 PNG bytes
- [ ] 5.4b **容器內字型可用性驗證（MUST 在 `docker compose exec api dotnet test` 實際容器內執行，不得僅在本機開發環境驗證）**：`CaptchaImageGeneratorTests` 新增或於既有測試中額外斷言不拋出 `SixLabors.Fonts.FontException`，且產出的 PNG 內容确实含有非純背景色的繪製痕跡（例如檢查像素不全為單一背景色，證明文字確實被繪製出來，而非因字型載入失敗而只產出空白圖片）——這是驗證 2.1a 內嵌字型在容器內（不依賴 OS 字型）確實可用的必要任務，6.1 僅驗證回傳非空 PNG bytes 不足以涵蓋此風險
- [ ] 5.4a `CaptchaRateLimitingOptionsTests`（`tests/ProjectC.Application.Tests/Common/`，逐行比照既有 `LoginRateLimitingOptionsTests.cs`）：驗證 `new CaptchaRateLimitingOptions()` 的預設值為 `PermitLimit = 10`、`WindowSeconds = 60`（CAPTCHA-RATE-002）
- [ ] 5.5 `LoginHandlerTests`／`RegisterMemberHandlerTests`／`JoinPurchaseQueueHandlerTests` 新增案例，改用 5.1 的 `FakeCaptchaService`（比照既有 Fake 型別的測試手法）：驗證碼錯誤時回傳 `Validation` 錯誤，且 MUST NOT 呼叫任何後續的帳密比對／唯一性檢查／排隊查詢或交易依賴（斷言對應 Fake 的呼叫次數為 0，對應 CAPTCHA-AUTH-001、CAPTCHA-REG-001、CAPTCHA-QUEUE-001）；缺漏 `CaptchaToken`／`CaptchaAnswer` 任一欄位時回傳 `Validation` 錯誤且不呼叫 `ICaptchaService.VerifyAsync`（CAPTCHA-AUTH-002、CAPTCHA-REG-002、CAPTCHA-QUEUE-002）；`CaptchaToken` 超過 64 字元／`CaptchaAnswer` 超過 16 字元時同樣回傳 `Validation` 錯誤且不呼叫 `VerifyAsync`（CAPTCHA-INPUT-001／002，三個 Validator 各自測一次即可，不需要重複三套完整案例）；`CaptchaAnswer` 原始字串超過 16 字元但 trim 後不超過（例如 13 個空白＋4 碼答案）時 MUST 通過長度驗證、繼續呼叫 `VerifyAsync`（CAPTCHA-INPUT-004，驗證 `RuleFor` lambda 內 trim 順序正確，三個 Validator 各測一次即可）；**任一 Handler 傳入已取消的 `CancellationTokenSource.Token` 呼叫 `HandleAsync`，斷言 `OperationCanceledException` 往外拋——這證明 Handler 把自己收到的 `cancellationToken` 轉傳給 `ICaptchaService.VerifyAsync`（5.1 的 `FakeCaptchaService` 已加上 `ThrowIfCancellationRequested()`），而不是誤用 `CancellationToken.None`（design.md 決策 10、CAPTCHA-CANCEL-003，三個 Handler 各測一次）**
- [ ] 5.5a **上述三個 Handler 的建構子新增 `ICaptchaService` 參數、`LoginRequest`／`RegisterMemberRequest` 新增必填欄位、`JoinPurchaseQueueHandler.HandleAsync` 新增 `JoinPurchaseQueueRequest` 參數後，這幾個既有測試檔案裡「本次改動之前就存在」的既有案例（非 5.5 新增的 CAPTCHA 案例）也會因為簽章改變而編譯失敗，MUST 同步修正、維持原本的 Assert 不變**，逐一列出（實作時確認實際檔案內容與行號，此處僅標注目前已讀取到的位置）：
  - `tests/ProjectC.Application.Tests/Authentication/Login/LoginHandlerTests.cs`：建構子呼叫新增 5.1 的 `FakeCaptchaService`（設為驗證永遠成功）；既有 4 處 `new LoginRequest(email, password)` 呼叫（對應「帳密正確登入成功」「密碼錯誤登入失敗」「Email 不存在登入失敗」既有情境）改為帶入 `FakeCaptchaService` 的固定有效 token／答案
  - `tests/ProjectC.Application.Tests/Members/Register/RegisterMemberHandlerTests.cs`：建構子新增 `FakeCaptchaService`；既有 3 處 `RegisterMemberRequest` 建構（對應「使用未重複 Email 註冊成功」「Email 已存在」「密碼不符強度規則」既有情境）新增正確的 `CaptchaToken`／`CaptchaAnswer`
  - `tests/ProjectC.Application.Tests/PurchaseQueue/JoinPurchaseQueue/JoinPurchaseQueueHandlerTests.cs`：`Fixture.CreateHandler()` 新增 `FakeCaptchaService` 依賴；既有 6 處 `fixture.CreateHandler().HandleAsync(@event.Id, memberId, CancellationToken.None)` 呼叫（`HandleAsync_FirstTimeJoining_*`／`HandleAsync_WhenAlreadyWaiting_*`／`HandleAsync_WhenAlreadyAdmittedAndNotExpired_*`／`HandleAsync_WhenExistingAdmittedEntryIsExpired_*`／`HandleAsync_WhenQueueModeIsDisabled_*`／`HandleAsync_WhenEventDoesNotExist_*`，對應 PQ-JOIN-001／002／003／005／006）改為多帶入帶正確驗證碼的 `JoinPurchaseQueueRequest`
  - `tests/ProjectC.Infrastructure.Tests/PurchaseQueue/JoinPurchaseQueueHandlerIntegrationTests.cs`：**此檔案直接 `new JoinPurchaseQueueHandler(...)` 並呼叫 `handler.HandleAsync(eventId, memberId, CancellationToken.None)`，完全不經過 HTTP／`WebApplicationFactory`／DI 容器，MUST NOT 誤用 6.2a／6.3 的 Fake 測試環境（那是 HTTP 層級的手法，這裡是直接建構）**——建構子直接傳入 `tests/ProjectC.Infrastructure.Tests/TestSupport/FakeCaptchaService.cs`（5.1 已說明三個測試專案各自一份，不是跨專案參照 Application.Tests 的型別）、兩處 `HandleAsync` 呼叫（`HandleAsync_WhenExistingAdmittedEntryIsExpired_PersistsBoth*`／`HandleAsync_WhenQueueModeIsDisabledByAdminAfter*`，對應 PQ-JOIN-003 與並發交易情境）改為多帶入帶正確驗證碼的 `JoinPurchaseQueueRequest`，維持原本針對 `ChangeTracker`／`ON CONFLICT` 行為的 Assert 不變
- [ ] 5.6 `GetCaptchaHandlerTests`：呼叫 `ICaptchaService.GenerateAsync` 並原樣回傳其結果（CAPTCHA-GEN-001）；傳入已取消的 `CancellationTokenSource.Token`，斷言 `OperationCanceledException` 往外拋，證明 Handler 把自己收到的 `cancellationToken` 轉傳給 `GenerateAsync`（design.md 決策 10、CAPTCHA-CANCEL-003）

## 6. 整合／元件測試

- [ ] 6.1 `CaptchaComponentTests`（`GET /api/captcha`，真實 Redis、真實 `RedisCaptchaService`）：成功回傳 token 與 base64 PNG、不含答案明文（CAPTCHA-GEN-001 的端點層級驗證，補足 5.6 僅測 Handler 呼叫轉發的缺口）；連續呼叫兩次回傳不同 token（CAPTCHA-GEN-002）
- [ ] 6.2 `CaptchaRateLimitTests`：短時間內大量呼叫 `GET /api/captcha` 觸發新增的 `captcha` policy（**不是 `login` policy**，見 design.md 決策 6），回傳 429 + `Retry-After`（CAPTCHA-RATE-001，比照既有 `LoginRateLimitingTests` 手法，需要用類似 `LoginRateLimitedWebApplicationFactory` 覆寫 `CaptchaRateLimiting:PermitLimit`／`WindowSeconds` 為小額度、短視窗的獨立 factory，不與其他測試共用同一組額度）；同一來源 IP 打滿 `captcha` policy 額度後，緊接著呼叫 `POST /api/auth/login` 仍可正常處理，反之亦然（CAPTCHA-RATE-004，證明兩個 policy 計數獨立）
- [ ] 6.2b `RateLimitingOptionsFailFastTests.cs`（`tests/ProjectC.WebApi.Tests/Startup/`）新增 `CaptchaRateLimiting` 的理論測試方法，逐行比照既有 `CreatingHost_WithNonPositiveLoginRateLimitingValues_ThrowsOptionsValidationException`：`CaptchaRateLimiting:PermitLimit`／`WindowSeconds` 設為 0 或負數時，建立 Host 拋出 `OptionsValidationException`（CAPTCHA-RATE-003）
- [ ] 6.2a **`CustomWebApplicationFactory`（`tests/ProjectC.WebApi.Tests/TestSupport/CustomWebApplicationFactory.cs`）MUST 新增：`ConfigureWebHost`／`ConfigureTestServices` 預設把 `ICaptchaService` 替換為 `tests/ProjectC.WebApi.Tests/TestSupport/FakeCaptchaService.cs`（此專案自己的一份，見 5.1；design.md 決策 9）**——這是本節其餘任務與所有既有整合測試能繼續運作的前提，不可省略。**已逐一核對每個實際引用 `CustomWebApplicationFactory`（含其子類別）的既有測試檔案，不是憑印象估計「20+」**：直接 `IClassFixture<CustomWebApplicationFactory>` 的有 16 個（`Admin/AdminEventsControllerTests.cs`、`Admin/AdminOrdersControllerTests.cs`、`Admin/AdminTicketsControllerTests.cs`、`Admin/AdminVenuesControllerTests.cs`、`Auth/AuthControllerTests.cs`、`BackgroundServices/ExpiredOrderCleanupServiceTests.cs`、`BackgroundServices/PurchaseQueueAdmissionServiceExecuteAsyncDelegationTests.cs`、`BackgroundServices/PurchaseQueueAdmissionServiceLeaderElectionBranchTests.cs`、`BackgroundServices/PurchaseQueueAdmissionServiceLeaderElectionTests.cs`、`BackgroundServices/PurchaseQueueAdmissionServiceTests.cs`、`Events/EventQueueControllerTests.cs`、`Events/EventsControllerTests.cs`、`Members/AdminMembersControllerTests.cs`、`Members/MembersControllerTests.cs`、`Orders/OrdersControllerTests.cs`、`Tickets/TicketsControllerTests.cs`）；另外 4 個透過子類別（`RedisUnavailableWebApplicationFactory`／`LogCapturingWebApplicationFactory`／`CachingComponentTestWebApplicationFactory`／`RateLimitedWebApplicationFactory`，皆繼承 `CustomWebApplicationFactory`）間接受影響：`Events/QueryCachingFailOpenTests.cs`、`Events/QueryCachingAccessScopeTests.cs`（也直接 `new CustomWebApplicationFactory()`）、`Events/QueryCachingComponentTests.cs`、`Orders/OrdersRateLimitingTests.cs`——合計剛好 20 個。只有其中透過 `AuthTestHelper` 呼叫註冊／登入 API 準備測試前置資料的檔案才會被驗證碼必填檢查影響，本身與驗證碼行為無關，一旦這兩個端點新增必填驗證碼檢查而不處理會因為請求缺漏欄位或答案無法程式化解出而全數壞掉（比照 `CustomWebApplicationFactory` 既有為了 `LoginRateLimiting:PermitLimit` 全域放寬到 1000 所留下的同類先例與註解）。同步修正：
  - `AuthTestHelper.RegisterAsync`／`LoginAsync`（`tests/ProjectC.WebApi.Tests/TestSupport/AuthTestHelper.cs`）：`RegisterMemberRequest`／`LoginRequest` 建構呼叫新增 `FakeCaptchaService` 的固定有效 token／答案
  - `EventQueueControllerTests.cs` 既有三個測試方法（`JoinQueue_AsAdminRole_Returns201AndCreatesEntry`／`JoinQueue_WithoutAuthentication_Returns401`／`JoinQueue_WhenRequestBodyCarriesAnotherMemberId_IgnoresItAndUsesCallersOwnJwtIdentity`，對應 PQ-JOIN-007／008／009）：呼叫 `/api/events/{id}/queue/entries` 時新增帶正確驗證碼的 `JoinPurchaseQueueRequest` body
  - **`AuthControllerTests.cs` 除了透過 `AuthTestHelper` 外，本身還有 6 處直接 `new LoginRequest(...)`／`new RegisterMemberRequest(...)`（此檔案的測試對象就是這兩個端點本身），這些呼叫全部 MUST 補上正確的驗證碼欄位，不能只改 `AuthTestHelper`**
  - **`Orders/OrdersRateLimitingTests.cs`（使用 `RateLimitedWebApplicationFactory` 子類別，已在上方受影響清單內）：本身有 1 處直接 `new LoginRequest(...)`（取得下單測試用的 Bearer Token），MUST 補上正確的驗證碼欄位**
  - **`Auth/LoginRateLimitingTests.cs` MUST NOT 遺漏——此檔案使用 `LoginRateLimitedWebApplicationFactory`（`tests/ProjectC.WebApi.Tests/TestSupport/LoginRateLimitedWebApplicationFactory.cs`），這個類別繼承 `WebApplicationFactory<Program>`，MUST NOT 繼承 `CustomWebApplicationFactory`（見該檔案既有註解：刻意不繼承，避免連帶繼承 Testcontainers 容器建構行為），是完全獨立的另一個 factory 階層，本任務對 `CustomWebApplicationFactory` 的修改完全不會套用到它。此檔案本身有 3 處直接 `new LoginRequest(...)`，且是本次改動風險最高的既有測試——它的測試目的就是連續送出大量登入請求觸發限流（LRL-001~010），一旦登入需要驗證碼卻沒有處理，這個檔案的核心測試手法會直接失效。MUST 在 `LoginRateLimitedWebApplicationFactory.ConfigureWebHost` 新增與 6.2a 相同的 `ICaptchaService` → 該專案自己的 `FakeCaptchaService` 替換，並補上 3 處呼叫的驗證碼欄位**
  - **`Observability/RequestTraceIdTests.cs`／`Observability/BackgroundServiceTraceIdTests.cs`／`Observability/SensitiveDataMaskingInStructuredPropertiesTests.cs` MUST NOT 遺漏——這三個檔案使用 `ObservabilityWebApplicationFactory`（`tests/ProjectC.WebApi.Tests/TestSupport/ObservabilityWebApplicationFactory.cs`），同樣直接繼承 `WebApplicationFactory<Program>`、不是 `CustomWebApplicationFactory` 的子類別，是第三個獨立的 factory 階層，同樣不會因為修改 `CustomWebApplicationFactory`而受益。`RequestTraceIdTests.cs` 本身有 2 處直接 `new LoginRequest(...)`，另兩個檔案透過 `AuthTestHelper` 呼叫註冊／登入。MUST 在 `ObservabilityWebApplicationFactory.ConfigureWebHost`（或其 `ConfigureTestServices`）新增與 6.2a 相同的 `ICaptchaService` 替換，並補上 `RequestTraceIdTests.cs` 的 2 處呼叫**
  - 6.1／6.2（本節，需要驗證**真實** `RedisCaptchaService` 行為）與 6.4（fail-closed，同樣需要真實服務才能觸發 Redis 連線例外）MUST 在各自的 `WebApplicationFactory`（子類別或 `ConfigureTestServices` 覆寫）把 `ICaptchaService` 換回真實的 `RedisCaptchaService`，比照 `NeedsWorkingRedis` 這種既有透過子類別覆寫改變基底行為的既定慣例，不依賴本任務新增的 Fake 預設值
- [ ] 6.3 `AuthControllerCaptchaTests`／`MemberControllerCaptchaTests`／`PurchaseQueueCaptchaTests`（HTTP 端到端）：直接使用 `CustomWebApplicationFactory`（6.2a 已讓它預設把 `ICaptchaService` 替換為 `FakeCaptchaService`，不需要為此另建專屬子類別），不呼叫真實 `GET /api/captcha` 也不需要猜測或讀取明碼答案：附帶 `FakeCaptchaService` 已知的固定 token／答案送出登入／註冊／加入排隊請求 → 成功；附帶錯誤答案 → 被拒絕、不建立任何資料、不核發任何 Token（CAPTCHA-AUTH-001/002、CAPTCHA-REG-001/002、CAPTCHA-QUEUE-001/002）。真正的雜湊、TTL、一次性語意已在 5.2／5.3 用真實 `RedisCaptchaService` 驗證，此處不重複驗證
- [ ] 6.3a **`PurchaseQueueCaptchaTests`（同 6.3 檔案或新檔案皆可）新增 PQ-JOIN-004 端到端案例**：對同一活動、同一已登入會員、尚無任何排隊紀錄，透過 `HttpClient` 用 `Task.WhenAll` 幾乎同時送出兩個 `POST /api/events/{id}/queue/entries` 請求，各自附帶 `FakeCaptchaService`（6.2a／6.3 已讓 `CustomWebApplicationFactory` 預設替換）已知的固定有效 token／答案（`FakeCaptchaService` 不追蹤一次性消耗，兩個請求重複使用同一組固定值不影響測試效度，一次性語意已在 5.2 CAPTCHA-VERIFY-006 用真實 Redis 驗證過）——斷言兩個 HTTP 回應皆成功（非 4xx/5xx）、兩者回傳的 entry Id 相同、資料庫（或查詢排隊狀態端點）確認最終只存在一筆進行中的排隊紀錄。這是 6.6 已標注的既有缺口：現有 `PurchaseQueueRepositoryConcurrencyTests.cs` 只測到 Repository 層，沒有任何測試涵蓋「驗證碼檢查＋Handler＋Controller」疊加後的並發加入排隊行為
- [ ] 6.4 `CaptchaFailClosedComponentTests`：使用比照既有 `RedisUnavailableWebApplicationFactory` 手法建立的 Redis 不可用測試環境（**MUST 在此 factory 內把 `ICaptchaService` 覆寫回真實的 `RedisCaptchaService`——不得沿用 6.2a 為其他既有測試新增的 `FakeCaptchaService` 預設值，否則測不到真正的 Redis 連線例外**），實際呼叫 `GET /api/captcha`，斷言回應為技術性錯誤（`5xx`，透過既有全域 `IExceptionHandler`），不回傳任何 token 或圖片（CAPTCHA-FAIL-001 的端到端可觀察行為）。**注意**：`RedisUnavailableWebApplicationFactory` 指向 `127.0.0.1:1`（無人監聽），驗證的是「TCP 連線被拒絕」這種快速失敗路徑，不是 CAPTCHA-FAIL-003「連線存在但命令逾時」這種路徑——後者已在 5.3（mock 拋出 `RedisTimeoutException`）與 5.3a（組態驗證）以可決定性重現的方式涵蓋，本任務 MUST NOT 額外嘗試用真實網路逾時重現 CAPTCHA-FAIL-003，避免引入時序不穩定的測試。CAPTCHA-FAIL-002 涉及登入、註冊、加入排隊三個 Controller／授權鏈／Handler 皆不同的端點，「任一端點」不能代表另外兩個，MUST 分別對三者各自建立測試案例，不得只測一個就視為涵蓋：
  - 登入端點（`POST /api/auth/login`）：Redis 不可用時呼叫，斷言回應為 `5xx` 技術性錯誤，不核發任何 Access／Refresh Token
  - 註冊端點（`POST /api/auth/register`）：Redis 不可用時呼叫，斷言回應為 `5xx` 技術性錯誤，不建立任何新會員
  - 加入排隊端點（`POST /api/events/{id}/queue/entries`）：Redis 不可用時呼叫，斷言回應為 `5xx` 技術性錯誤，不建立任何排隊紀錄
  - 上述三者 MUST 各自額外斷言：其回應狀態碼與型態明確不同於「驗證碼答案錯誤」情境下的一般 `Validation`／`400` 回應（不得兩者外觀相同、無法區分），不因 Redis 不可用而放行成功（對應 design.md 決策 5「MUST NOT 包裝成業務錯誤」）
- [ ] 6.5 `PurchaseQueueStatusEndpointTests` 既有測試補一個案例：查詢排隊狀態端點不需要驗證碼欄位仍可正常回應（CAPTCHA-QUEUE-003）
- [ ] 6.6 既有 HTTP 端到端測試依 CustomWebApplicationFactory（6.2a，經由 `AuthTestHelper` 已全域補上正確驗證碼）與 `EventQueueControllerTests.cs`（6.2a 已個別補上）的修正應自動維持通過，不需要再逐一改動；Application／Infrastructure 單元測試層級的既有案例修正已列於 5.5a，不在此重複。此處逐一列出需要另外確認的既有 Scenario 測試覆蓋現況，每一條 MUST 個別確認、不得以籠統的「PQ-JOIN-*」「既有測試」帶過：
  - `authentication` spec「帳密正確且帳號啟用時登入成功」「密碼錯誤時登入失敗」「Email 不存在時登入失敗」：對應 `LoginHandlerTests.cs`（見 5.5a），Handler 層級已覆蓋
  - `member-management` spec「使用未重複的 Email 註冊成功」「使用已存在的 Email 註冊失敗」「密碼不符強度規則」：對應 `RegisterMemberHandlerTests.cs`（見 5.5a），Handler 層級已覆蓋
  - `purchase-queue` spec PQ-JOIN-001／002／003／005／006：對應 `JoinPurchaseQueueHandlerTests.cs`（見 5.5a），Handler 層級已覆蓋；PQ-JOIN-003 另有 `JoinPurchaseQueueHandlerIntegrationTests.cs` 的 Handler＋真實 DB 層級覆蓋（見 5.5a，已改帶正確驗證碼）
  - **`purchase-queue` spec PQ-JOIN-004（兩個請求同時首次加入排隊）：目前唯一的既有測試是 `PurchaseQueueRepositoryConcurrencyTests.cs`，且該測試只直接呼叫 `PurchaseQueueRepository.AddOrGetExistingAsync`，完全不經過 `JoinPurchaseQueueHandler`、不經過驗證碼檢查、不經過 Controller/HTTP——這確實驗證了資料庫層的並發衝突處理機制本身（唯一性約束、`ON CONFLICT DO NOTHING`、重試一次），但無法證明「兩個各自帶正確驗證碼的完整 HTTP 請求同時加入排隊」這條 AC 描述的端到端行為，也無法驗證驗證碼檢查這一層不會在並發情境下產生非預期的交互作用（例如誤判其中一個為驗證碼錯誤）。此既有測試不需要修改（不受簽章變更影響），但不足以視為 PQ-JOIN-004 這條 AC 已有完整覆蓋，MUST 另外新增一項端到端測試，見 6.3a**
  - `purchase-queue` spec PQ-JOIN-007／008／009：對應 `EventQueueControllerTests.cs`（見 6.2a），HTTP 層級已覆蓋
  - `buyer-web-ui` spec「註冊成功」：**目前沒有對應的 `RegisterPage.test.ts`（frontend 元件測試，此檔案目前不存在）**，見 7.2 新建任務，不得誤以為此情境已有既有測試涵蓋
  - `buyer-web-ui` spec「登入成功導向買家端首頁」：**目前 `LoginPage.test.ts` 只覆蓋 429／非 429 失敗兩案例，沒有成功路徑的既有測試**，見 7.2 新增任務
  - `buyer-web-ui` spec「登入失敗顯示錯誤訊息」：對應 `LoginPage.test.ts` 既有「登入失敗（非 429）」案例，見 7.2，補上驗證碼已正確填寫的前提（`fillAndSubmit` 輔助函式）
  - `buyer-web-ui` spec「LRL-009 登入因請求頻率限制被拒絕」：對應 `LoginPage.test.ts` 既有 `LRL-009` 案例，見 7.2，同上補上 `fillAndSubmit` 的驗證碼欄位（此案例走 429 短路，驗證碼欄位是否正確不影響 429 判斷，但欄位缺漏會在更早的 Validation 階段被拒絕，仍須補上避免請求本身先被判定為缺漏驗證碼而非命中限流）
  - **`buyer-web-ui` spec 排隊相關既有 AC（BW-QUEUE-001~006、BW-TOGGLE-001）逐條 regression test mapping**——全部位於 `EventDetailPage.test.ts` 的 `describe('EventDetailPage 熱門搶購模式排隊（本次新增）')` 區塊，測試類型皆為 Vitest 元件測試（`@vue/test-utils` `mount`）：

    | AC | 既有測試方法（節錄） | 操作步驟 | CAPTCHA 前置條件 | 預期斷言（含本次需新增的部分） | 是否受本次改動影響 |
    |---|---|---|---|---|---|
    | BW-QUEUE-001 | `BW-QUEUE-001：進入熱門搶購模式活動且尚未加入排隊，顯示加入排隊操作、不開放選位` | mock `getMyQueueStatus` 首次回傳 `NotJoined`；掛載頁面；**新增**：填寫驗證碼輸入欄位；點擊「加入排隊」按鈕 | **受影響**：畫面會顯示驗證碼圖片與輸入欄位（CAPTCHA-BW-QUEUE-001），送出前 MUST 先填寫，否則若前端做必填驗證會卡在原地送不出去 | 顯示「加入排隊」文字；不顯示 `.seat-btn`；點擊後 `queueApi.joinQueue` 被呼叫且**改為斷言包含 `captchaToken`／`captchaAnswer` 兩個新參數**（`web/src/api/queue.ts` 簽章變更）；畫面顯示「排隊中」 | **是，MUST 更新（見 7.3a）** |
    | BW-QUEUE-002 | `BW-QUEUE-002：排隊中顯示等待畫面與前方等待人數，停用選位/計數/區域隨選，並定期輪詢狀態` | mock `getMyQueueStatus` 直接回傳 `Waiting`（不經過 `NotJoined`）；掛載頁面；推進計時器 | 不需要——元件掛載時直接進入 `Waiting` 分支，不會渲染「尚未加入排隊」畫面，不會呼叫 `GET /api/captcha` | 顯示「排隊中」與前方人數；`.seat-btn`／自動選位按鈕不存在；`getMyQueueStatus` 依計時器被呼叫第二次 | **否——已核對測試本體不經過驗證碼分支，不需修改** |
    | BW-QUEUE-003 | `BW-QUEUE-003：排隊狀態由 Waiting 轉為 Admitted 後，停止輪詢並開放選位與下單` | mock `getMyQueueStatus` 依序回傳 `Waiting` → `Admitted`；推進計時器 | 不需要——全程未經過 `NotJoined` 分支 | 停止輪詢（呼叫次數不再增加）；`.seat-btn` 出現 | **否——已核對，不需修改** |
    | BW-QUEUE-004 | `BW-QUEUE-004：下單時排隊資格已逾時（403 + QueueAdmissionRequired），導回排隊等待畫面...` | mock `getMyQueueStatus` 首次回傳 `Admitted`；下單 API mock 回傳 403/QueueAdmissionRequired；選座位送出訂單 | 不需要——直接以 `Admitted` 起始，不經過 `NotJoined` 分支，也不呼叫 `joinQueue` | 不顯示「下單失敗」；顯示「排隊中」；座位/票種查詢 API 各只呼叫 1 次（不重新整理） | **否——已核對，不需修改** |
    | BW-QUEUE-005 | `BW-QUEUE-005：下單因請求頻率限制被拒絕（429），顯示提示、不清空已選內容、不重新整理資料` | mock `getMyQueueStatus` 回傳 `Admitted`；下單 API mock 回傳 429；選座位送出訂單 | 不需要——同上，不經過 `NotJoined` 分支 | 顯示「請求過於頻繁」；已選座位數不變；查詢 API 不重新呼叫 | **否——已核對，不需修改** |
    | BW-QUEUE-006 | `BW-QUEUE-006：其他語意的 403（title 不是 QueueAdmissionRequired）沿用一般失敗處理...` | mock `getMyQueueStatus` 回傳 `Admitted`；下單 API mock 回傳其他語意 403；選座位送出訂單 | 不需要——同上 | 顯示該 403 的錯誤訊息；已選座位清空；查詢 API 重新呼叫（一般失敗處理） | **否——已核對，不需修改** |
    | BW-TOGGLE-001 | `BW-TOGGLE-001：排隊等待期間 Queue Mode 被關閉，下一次輪詢的 queueModeEnabled 為 false 時，停止輪詢並開放正常購票` | mock `getMyQueueStatus` 首次回傳 `Waiting`＋`queueModeEnabled: true`，第二次回傳同狀態但 `queueModeEnabled: false`；推進計時器 | 不需要——全程 `Waiting`，不經過 `NotJoined` 分支 | 停止輪詢後不再顯示「排隊中」；`.seat-btn` 出現；輪詢次數不再增加 | **否——已核對，不需修改** |

    「不受影響」的判定依據：這 6 個既有測試皆用 `vi.mocked(queueApi.getMyQueueStatus).mockResolvedValueOnce(...)`／`mockResolvedValue(...)` 讓元件掛載時直接進入 `Waiting`／`Admitted` 分支，完全不會渲染「尚未加入排隊、需要顯示驗證碼」的畫面，也不會呼叫 `queueApi.joinQueue`——這是已核對過實際測試檔案內容（非假設）得出的結論，逐條列在此表是為了滿足「每條 AC 至少對應一項測試任務」的可追溯要求，不是用一句「既有測試不受影響」帶過

## 7. 前端測試／手動驗證

- [ ] 7.1 `useCaptcha` composable 單元測試（Vitest）：呼叫 API 成功後正確暴露 token/imageBase64；`refresh()` 換發新值；API 呼叫失敗（429／5xx）時設定 `loadError` 為固定提示文字、不拋出例外中斷呼叫端（CAPTCHA-BW-005／006）；後續呼叫成功後 `loadError` 清空
- [ ] 7.2 註冊頁／登入頁元件測試：
  - **`RegisterPage.test.ts`（目前不存在，MUST 新建）**：載入時顯示驗證碼圖片與輸入欄位（CAPTCHA-BW-001）；提供正確 Email／密碼／驗證碼送出後呼叫註冊 API 成功並導向登入頁（既有「註冊成功」情境，目前完全沒有自動化元件測試涵蓋，見 6.6）；驗證碼錯誤時顯示提示且自動換發（CAPTCHA-BW-002）；手動刷新按鈕觸發換發（CAPTCHA-BW-003）；初次載入 `GET /api/captcha` 失敗時顯示提示並停用送出按鈕（CAPTCHA-BW-005）；刷新失敗時顯示提示、停用送出按鈕、不清空已填的 Email／密碼欄位（CAPTCHA-BW-006）
  - **`LoginPage.test.ts`（既有檔案）**：既有 `fillAndSubmit` 輔助函式 MUST 新增填入驗證碼輸入欄位這一步——否則若驗證碼欄位為前端必填（native/Vue 驗證），既有測試送出表單時會被卡在前端驗證，根本不會呼叫到 mock 的 `loginMock`，既有斷言會意外落空而非驗證原本要測的情境；新增「提供正確帳密與驗證碼送出後呼叫登入 API 成功並導向買家端首頁」案例（既有「登入成功導向買家端首頁」情境，目前 `loginMock` 只被 mock 為失敗，沒有成功路徑的既有測試，見 6.6）；既有 `LRL-009` 與「登入失敗（非 429）」兩案例維持原本斷言不變，僅補上 `fillAndSubmit` 的驗證碼欄位；新增載入時顯示驗證碼圖片與輸入欄位案例（CAPTCHA-BW-004）；驗證碼錯誤時顯示提示且自動換發（CAPTCHA-BW-002）；手動刷新按鈕觸發換發（CAPTCHA-BW-003）；初次載入失敗（CAPTCHA-BW-005）與刷新失敗（CAPTCHA-BW-006）比照 `RegisterPage.test.ts` 同樣補上
- [ ] 7.3 活動詳情頁「加入排隊」元件測試（`EventDetailPage.test.ts`）：尚未加入排隊時顯示驗證碼圖片與輸入欄位（CAPTCHA-BW-QUEUE-001）；驗證碼錯誤時 MUST 同時斷言以下四項，缺一不可（CAPTCHA-BW-QUEUE-002 完整 AC，不能只驗證其中一部分就視為涵蓋）：①顯示驗證碼錯誤提示、②自動呼叫 `GET /api/captcha` 換取新驗證碼並清空舊有輸入欄位（斷言輸入框的值已被清空，不是只斷言「呼叫過 `GET /api/captcha`」）、③停留在尚未加入排隊的畫面、④選位與計數購票操作入口仍不開放（比照 BW-QUEUE-001 對「不開放選位/計數購買輸入」的既有斷言手法）；`GET /api/captcha` 載入或刷新失敗時顯示提示、停用加入排隊按鈕，不清空/重新整理座位與票種資料（沿用 CAPTCHA-BW-005／006 的共用行為）
- [ ] 7.3a **更新 `EventDetailPage.test.ts` 既有的 `BW-QUEUE-001` 測試案例（`describe('EventDetailPage 熱門搶購模式排隊（本次新增）')` 內）**：新增填寫驗證碼輸入欄位這一步再觸發「加入排隊」按鈕點擊（否則若前端要求驗證碼為必填，既有測試按鈕點擊可能被前端驗證擋下、根本不會呼叫到 `queueApi.joinQueue`）；既有斷言 `expect(queueApi.joinQueue).toHaveBeenCalledWith('event-1')` 改為包含新增的 `captchaToken`／`captchaAnswer` 參數，其餘既有斷言（顯示「加入排隊」、不顯示座位按鈕、成功後顯示「排隊中」）維持不變；同一 `describe` 區塊內 BW-QUEUE-002~006、BW-TOGGLE-001 六個既有測試已確認不受影響（見 6.6），不需修改
- [ ] 7.4 真實瀏覽器手動驗證（claude-in-chrome 或手動）：註冊、登入、加入排隊三個流程實際看得到驗證碼圖片、正確驗證碼可通過、錯誤驗證碼被拒絕並換新圖

## 8. 文件與收尾

- [ ] 8.1 archive 階段同步更新 `docs/project-scope.md`：第 2 節 Could 項「動態驗證碼（CAPTCHA）」標記完成狀態，並於第 5 節「安全」小節註明已補上驗證碼防護。**標記完成狀態時 MUST 比照第 48 行「登入 Rate limiting」既有寫法的措辭格式，明確交叉參照 CAPTCHA 專用的 `captcha` rate-limit policy 與 `api-rate-limiting` 的關聯**（獨立 policy、獨立 `CaptchaRateLimitingOptions`，但共用 `Microsoft.AspNetCore.RateLimiting` 既定機制與回應格式，不共用計數，見 design.md 決策 6），不得只打勾完成狀態、略過這段關聯說明——`openspec/specs/` 底下的 capability 資料夾彼此沒有索引串連，`docs/project-scope.md` 是唯一承擔跨 capability 關聯說明角色的既有位置，日後搜尋限流行為時才不會漏掉 CAPTCHA endpoint
